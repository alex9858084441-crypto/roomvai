# --- Основной API ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9  # приём multipart/form-data (загрузка фото)

# --- Работа с изображениями ---
Pillow>=10.0,<12.0

# --- Supabase (server-side: service_role операции) ---
supabase>=2.5.0

# --- Replicate API ---
replicate>=0.30.0

# --- Тесты ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27  # для TestClient
