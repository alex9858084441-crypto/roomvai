# --- Основной API ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9  # приём multipart/form-data (загрузка фото)

# --- Работа с изображениями ---
Pillow>=10.0,<12.0

# --- ML (опционально, для режима ML_MODE=sd) ---
# Тяжёлые зависимости; ставьте отдельно, если нужна реальная генерация:
#   pip install torch torchvision
#   pip install diffusers transformers accelerate

# --- Тесты ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27  # для TestClient
